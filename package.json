{
  "name": "undersea-core",
  "version": "1.0.0",
  "main": "index.js",
  "author": "wuwe1 <wu_we1@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "circomlib": "^2.0.5",
    "hardhat": "^2.9.9",
    "hardhat-circom": "^3.3.0"
  },
  "scripts": {
    "circom:dev": "hardhat circom --deterministic --debug --verbose",
    "circom:prod": "hardhat circom --verbose",
    "circom:verify": "cd ./scripts && ./verify.sh"
  }
}
